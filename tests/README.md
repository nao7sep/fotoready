# FotoReady's areas, and the tests that stand for them

`npm test` is the type check plus this whole suite, minus `live/`: at a few seconds it is already a
fixed, balanced run, so nothing selects a subset of it. `npm run test:full` adds `live/`, which
builds the app into `node_modules/.cache/fotoready-live` and drives the real IPC handlers the
renderer calls — the pipeline worker as the build ships it, the real ExifTool, and paid calls to the
real Gemini API — one file at a time, on a throwaway app home, against the shared fixture corpus.

This file is the balance judgement the `tests-folder-conventions` require — which areas FotoReady
has, and which tests stand for each — so a reader can tell what a green run covered, and an area with
no test standing for it is visible rather than merely absent. `tests/area-map.test.ts` holds every
path below to what is on disk.

Paths are relative to this folder. Every non-live test file appears exactly once below, apart from
`area-map.test.ts` itself, which guards the table rather than standing for an area of the app — so
the map accounts for the whole default run rather than sampling it.

| Area | What it covers | Tests standing for it |
|---|---|---|
| The operation stack | The reorderable non-destructive edits: which parameters each registered op accepts, and the geometry those edits and their overlays compute with. | `core/ops/validate.test.ts`, `core/ops/crop-region.test.ts`, `shared/validation/ops.test.ts`, `shared/box-geometry.test.ts`, `shared/rotation.test.ts`, `shared/numeric.test.ts` |
| The render pipeline | Decode, the ops in order, and the re-encode — in the worker the build ships — plus the output format and quality each task resolves to, and the shorter pipeline a live preview runs. | `main/workers/pipeline-worker.test.ts`, `runtime/encode.test.ts`, `runtime/jpeg-quality.test.ts`, `shared/output-format.test.ts`, `shared/preview-pipeline.test.ts`, `shared/validation/pipeline.test.ts`, `main/task-output.test.ts` |
| Queues and pipeline failure | Scheduling saves and vision runs off the main thread, cancelling them, and turning a failed run into a category the app can retry or report. | `main/processing-queue.test.ts`, `runtime/pipeline-error.test.ts`, `main/queues/vision-error.test.ts` |
| Metadata control | Which metadata fields survive the re-encode — preserved, stripped, or injected — what the privacy warning flags, how edited fields are cleaned, and how they persist on a saved task. | `main/metadata-flags.test.ts`, `main/metadata-policy.test.ts`, `main/privacy-warning.test.ts`, `main/session-metadata-persistence.test.ts`, `shared/text-cleanup.test.ts` |
| Batch rename and slugs | Collision-safe rename templates, the slug rules feeding them, the filenames they are allowed to render, and the rename dialog itself. | `main/rename-service.test.ts`, `shared/rename-template.test.ts`, `shared/validation/filename-template.test.ts`, `core/slug/rules.test.ts`, `renderer/components/modals/rename-modal.test.ts` |
| AI assist | The opt-in Gemini path: where the key is stored, which run mode a request resolves to, and how a refused or truncated response reports the provider's own reason. | `adapters/gemini-refusal.test.ts`, `adapters/api-keys.test.ts`, `shared/vision-run-mode.test.ts` |
| LUTs, stamps, and overlay assets | The built-in and user-imported asset catalogues, the `.cube` parser and sampler behind LUTs, and the op cards and picker that choose an asset. | `main/stamp-catalog.test.ts`, `main/builtin-stamp-catalog.test.ts`, `main/file-asset-catalog.test.ts`, `runtime/lut-cube.test.ts`, `renderer/stamp-filter.test.ts`, `renderer/components/modals/asset-picker-modal.test.ts`, `renderer/ops/lut.test.ts`, `renderer/ops/stamp.test.ts`, `renderer/ops/watermark-image.test.ts`, `renderer/ops/asset-overlay.test.ts` |
| The project session | Originals and tasks in a session: importing files dropped on the window, what selection survives a removal, and the `.json` sidecar a save writes and a drop re-imports. | `main/session-original-import.test.ts`, `main/session-selection.test.ts`, `main/task-removal.test.ts`, `main/task-sidecar-import.test.ts`, `shared/task-sidecar.test.ts`, `renderer/original-import-feedback.test.ts`, `renderer/app-file-drop.test.ts`, `renderer/external-file-drop.test.ts` |
| Settings, state, and durable writes | Reading, validating, and materializing `config.json` and `state.json`, recovering from a corrupt one, saving a settings edit, and the atomic write and backup store that keep those files whole. | `main/settings-io.test.ts`, `main/settings-corrupt-e2e.test.ts`, `main/state-io.test.ts`, `shared/validation/settings.test.ts`, `shared/validation/state.test.ts`, `shared/validation/common.test.ts`, `renderer/settings-save.test.ts`, `renderer/components/modals/settings-modal.test.ts`, `adapters/atomic-file.test.ts`, `main/backup-store.test.ts` |
| Storage roots and output paths | Where the app reads and writes: the storage root, the per-kind app paths, the `FOTOREADY_HOME` override, and the output directory a project saves into. | `main/paths.test.ts`, `main/storage-root.test.ts`, `main/output-paths.test.ts` |
| The window, startup, and app dialogs | Window options, minimum size, restoring persisted bounds, the activity signal the renderer follows, and what the app shows while it starts or when it must speak in a native dialog. | `main/window-options.test.ts`, `main/window-minimum.test.ts`, `main/window-state-recovery.test.ts`, `main/window-activity.test.ts`, `renderer/window-activity.test.ts`, `main/startup-dialog.test.ts`, `main/plain-message-dialog.test.ts`, `renderer/components/startup-load-gate.test.ts`, `renderer/components/modals/about-modal.test.ts` |
| Theme and styling | Following or overriding the OS appearance, and the stylesheet contract the themes are drawn from, including the contrast floor every color pair must clear. | `main/theme.test.ts`, `renderer/styles.test.ts`, `renderer/theme-contrast.test.ts` |
| The editing workspace | The canvas that shows the preview and its histogram, the ops panel beside it, and the pane geometry that keeps them usable as the window resizes. | `renderer/components/canvas/editor-canvas.test.ts`, `renderer/components/canvas/histogram-overlay.test.ts`, `renderer/components/panels/ops-panel-layout.test.ts`, `renderer/layout/workspace-layout.test.ts`, `shared/layout/workspace-metrics.test.ts` |
| Modals, keyboard, and composite controls | The modal stack and its focus trap and scroll lock, list and tab navigation, and the text-entry rules that keep shortcuts and IME composition out of each other's way. | `renderer/components/modals/focus-trap.test.ts`, `renderer/components/modals/modal-stack.test.ts`, `renderer/components/modals/scroll-lock.test.ts`, `renderer/components/composite-nav.test.ts`, `renderer/components/useListbox.test.ts`, `renderer/utils/editing-target.test.ts`, `renderer/utils/ime-guard.test.ts` |
| Failure reporting and logging | What the user is told when an action fails, which failures a surface owns, the error boundary that catches the rest, and the session log both processes write to. | `main/logger.test.ts`, `renderer/renderer-log.test.ts`, `renderer/present-failure.test.ts`, `renderer/owned-failures.test.ts`, `renderer/components/error-boundary.test.ts` |
| Build, packaging, and the shipped shell | The build configuration, the double-clickable launchers, the installer and the license texts it carries, and the content security policy the renderer loads under. | `config/electron-vite.test.ts`, `config/launcher-runtime.test.ts`, `main/installer-config.test.ts`, `scripts/content-security-policy.test.ts` |
